version: "3.9"

services:

  backend:
    build: ./backend
    container_name: lumina_backend
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      DB_HOST: host.docker.internal
      DB_USER: app
      DB_PASSWORD: 'Ankit@261,'
      DB_NAME: authapp
      JWT_SECRET: efb10c20a5072ff374a9b3c1903fa4aac9520cb3acd8fdf0ec4137767c77f178
      ADMIN_EMAIL: admin@lumina.com
      ADMIN_PASSWORD: Admin@123
    ports:
      - "8000:8000"
    networks:
      - app

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: http://54.234.186.64:8000
    container_name: lumina_frontend
    restart: always
    depends_on:
      - backend
    ports:
      - "80:3000"
    networks:
      - app

networks:
  app:
